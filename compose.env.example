# Copy to compose.env (gitignored): cp compose.env.example compose.env
# Then: docker compose up --build

JWT_SECRET=replace-with-openssl-rand-hex-32

# Bundled MongoDB in docker-compose. For Atlas, replace with your connection string.
MONGO_URI=mongodb://mongo:27017/subvet
